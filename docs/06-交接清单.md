# 交接清单

状态截至 2026-08-25 17:50（太平洋时间）。

## 当前状态（2026-08-25 21:30）

**没有训练在跑**，所有卡已释放。

| 产物 | 位置 | 说明 |
|---|---|---|
| 预训练适配器 1–7 轮 | `pretrain/outputs/e8/checkpoint-{199…1393}` | 每 199 步一个 epoch；最优点 epoch 2 |
| 预训练 1 轮 / 3 轮 | `pretrain/outputs/e1`、`e3` | 早期跑次 |
| 修复微调 A（接 3 轮预训练） | `sft_repair/outputs/pt_then_sft/` | 710 步，最低 2.9049 @ 步350 |
| 修复微调 B（接 2 轮预训练） | `sft_repair/outputs/pt2_then_sft/` | 950+ 步，最低 **2.8761** @ 步350 |
| 第二批标注任务 | `preference/标注_第二批.html` | 80 对，等主人标 |
| 训练旁路采样 | `pretrain/wandb_sample_watcher.py` | 每出一个 checkpoint 就生成几首诗打到 wandb |

## 已发生的事故

2026-08-25 17:34–17:35，两个训练在不同卡上相隔 90 秒同时崩溃
（`CUDA error: an illegal memory access`），指向机器层面事件。
修复版微调因 `save_strategy: epoch` 未留下任何 checkpoint，21 分钟作废，已从头重启并
改为每 50 步存一次；8 轮预训练从 `checkpoint-398`（第 2 个 epoch）续跑。
详见 `05-维护手册.md`。

## 已完成的产物（都在服务器，不入公开仓库）

| 内容 | 位置 | 说明 |
|---|---|---|
| 预训练适配器 1 轮 / 3 轮 | `pretrain/outputs/e1`、`e3` | 513 MB each |
| 修复后的微调数据 | `sft_repair/sft_train_v2.json` + `llamafactory/` | 2836 训练 / 152 开发 |
| 重新生成的改写草稿 | `sft_repair/flat_drafts.jsonl` | 818 条 |
| 偏好样本池 | `preference/bank.jsonl` | 764 条（去重后） |
| 标注页面 | `preference/标注.html` | 主人正在标，131 对 |
| 同题对照阅读件 | `pretrain/对照阅读.md` | 12 组，基座 vs 预训练 |
| 失败归因三份报告 | `reports/*.txt` | 见 `scripts/analysis/` |
| 脚本指纹 | `SCRIPT_FINGERPRINTS.txt` | 结论所依据的 codex 脚本版本 |

## 等主人的两件事

1. **第二批标注（80 对）的导出 JSON**。它同时决定 DPO 的基础模型
   （A 部分 24 对）和 DPO 数据长什么样（B 部分 56 对同模型采样）。
2. 见下。

## 另一件等主人的事

**`luofu.epub` 源文件**（如果还找得到）。501 首洛夫作品的真实分节只能从它恢复，
现有数据里已被抹平。

第一批偏好标注（131 对）已于 2026-08-25 完成并分析，结论见
`04-偏好标注与DPO.md`——它推翻了 AI 评审的判断，也推翻了 v3 的立论。
大规模标注等模型定型后再做，规模建议 500 对可用（约需标注 850 对）。

## 悬着的决定（都不是我该替主人做的）

| 决定 | 现状 | 我的建议 |
|---|---|---|
| 系统提示词与全部样本矛盾 | 保留原样未改 | 危害小于其他四处；要改需连同 10 个引用它的脚本与评测一起改 |
| `--min-quality` 当准入门 | 仍是门（≥ −8.0） | 改成只排序不当门——实测准确率 75%，当门会砍掉约四分之一主人会选的东西 |
| v3 路线是否继续 | codex 仍在推进 | **立论已被标注推翻**：它的 target 由基座自采样，而基座在盲标中 62 场只赢 2 场 |
| 我提的「可行性筛」（408 题砍到 208 题） | 未采纳 | **建议不要采纳**，它会砍掉极短行那一端，正是主人喜欢的写法 |
| `flat_rewrite` 的 771 条要不要进最终训练 | 已进 | 草稿已重做，重合率 0.02 → 0.32，可用 |
| 数据集 43 项检查缺分节合理性检查 | 未补 | 建议补，否则同类排版问题还会漏过去 |

## 给接手者的三条提醒

1. **codex 在实时改 `cyberpoet_v1`**。只读，不改，结论写成新文件交接。
2. **不用模型评价文学性**，包括不要让我来评。只报可数的，诗交给人读。
3. **看到空卡立刻占**，别套 `gpu_gate.py` 排队。

## 大文件怎么交换

外网流量吃紧，**不要直接传输大文件**。诗歌正文与权重也不能进公开仓库
（版权仍在有效期）。走私有中介：数据集本身就是从 Google Drive 取的
（`CyberPoet_poetry_dataset_v0.3.0/DOWNLOAD_PROVENANCE.json` 里有出处与 sha256），
新产物同样放 Drive，仓库里只留文档、脚本、配置与哈希。
