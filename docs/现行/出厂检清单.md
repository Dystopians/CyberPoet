# 出厂检清单（2026-09-16 起，一处为准）

任何新臂或新桥上卷之前逐项过。「依据等级」：已证 = 两条以上独立证据或干预实验；削弱 = 方向成立但数字/口径部分不成立；待证/待裁 = 只有推断，或口径等主人定。

| # | 查什么 | 用什么 | 通过线 | 依据等级 | 出处 |
|---|---|---|---|---|---|
| 1 | 训练提示与推理提示逐 token 一致 | 对比 `template: qwen3 + enable_thinking: false` 的训练样本与 `apply_chat_template(enable_thinking=False)` | 逐 token 相同（含空 think 块） | 已证 | 归因报告 4.4 |
| 2 | 权重差分（新训练方式首次落地必做） | `scripts/audit/check_weight_diff.py`（中途只查已轮到层；段末全谱） | 未训签名 = 零变化率 >70% **且** ‖Δ‖/‖W‖ <5e-4；单看占比不熔断；输出头三件 layer 模式下预期冻结只记不判 | 已证（09-02） | 证据链 11、STATE 09-02 |
| 3 | DPO 参考模型是什么 | 读配置：LoRA 下 `disable_adapter` = 裸底座，须先 `merge_bridge.py` 合并桥再新建 LoRA | 训练日志第 5 步 rewards/chosen ≈ 0、loss ≈ 0.693 | 已证 | 归因报告 4.1、5 |
| 4 | 训前偏好对打分 | `scripts/audit/score_pairs_trainfmt.py` + `analyze_margins.py` | 平均梯度权重 ≈ 0.5；加权后正例−反例字数差绝对值 ≤15 | 削弱（原脚本模板缺陷，重算中） | 归因报告 4.1、附录 A |
| 5 | SFT 段对照面板（198 首） | `gen_panel_arm.py` 分别出 DPO 前后 | 报字数中位、字/行、碎行率、短于 60 字数、标点、重复行；DPO 后不得腰斩 | 已证（09-02） | STATE 09-02 |
| 6 | 复读率 | `scripts/audit/degeneracy.py` 或 198 首面板 | **口径待主人裁**：建议 198 首面板、重复罚 1.08、整首循环 >5% 不出卷；关重复罚只做诊断；测题不得在桥的 SFT 训练集内 | 待裁 | 归因报告 4.3、附录 F/G |
| 7 | 记忆核验 | `check_memorization.py` 对全量预训练语料 | ≥20 窗一律否决 | 已证（09-02） | 卷宗 README |
| 8 | 面板题面不与训练题重叠 | 对照该臂的 DPO/SFT 题面 | 重叠 0 | 新增（09-16） | 归因报告 6 |
| 9 | 面板数字标口径 | — | 每张表注明重复罚、模板、dev 版本、题面是否重叠 | 新增（09-16） | 维护手册 |
| 10 | DPO 验证集 | 建对时按题面留 40 对 | 记录每轮 eval margin / accuracy，取分叉点 | 待裁 | 归因报告 7 |
| 11 | 撞顶率 | 面板里 ≥555 token 的比例 | 报数（现 max_new_tokens 560 剪掉 5–6% 长诗） | 新增（09-16） | 复核记录 H1 |
| 12 | 开发集口径 | 预训练 dev 按篇全量算；SFT dev 去掉预训练已见篇目 | 重算前不用 dev 定轮数、不用 SFT dev 排桥 | 待证 | 归因报告 4.3 |
| 13 | 量化是否真的生效 | 训练日志里找 `Quantizing model to 4 bit with bitsandbytes` 一行；配置写 `quantization_method: bnb`（写成 `bitsandbytes` 不报错、直接不量化） | 有那一行；显存约 13 GB（没量化是 28–35 GB）；每个臂在它训练时的精度下推理 | 已证（09-21：此前 47 份训练日志无一量化） | 裁决日志 09-21 |
