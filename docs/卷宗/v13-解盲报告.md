# v13 解盲报告（2026-09-22 解码；卷子 09-08 交付）

答卷：57/57 表态，「都不要」24（42%），批注 3 条。判决只认票。头条按 09-21 新口径。

## 一、一句话

**把偏好训练的参考模型改回桥（M7R），在你眼里什么都没改变：M7R 对 M7 决定票 1 : 2，18 局里 15 局「都不要」（83%）；M7R 对现役 M4 2 : 5，15 局里 8 局都不要。真伪题三臂 1/23 决定票（M7 一首《电视》胜马雁），M7R 0/11。** 两条预登记（对 M7 ≥55%、对 M4 ≥50%）都未命中。

## 二、都不要率（头条）

| 局型 | 都不要 |
|---|---|
| 真伪 ha | 1/24 = 4%（多多 1 局） |
| 机机 aa | **23/33 = 70%**：M7R 对 M7 15/18 = 83%；M7R 对 M4 8/15 = 53% |

M4 在场的机机局 53% 都不要——与 v10b 55%、v11 58%、v14 47% 同级（M4 是同一个模型，这条线是你的尺子，见 v14 报告第七节）。

## 三、机机对决（附注）

| 对局 | 决定票 | 结果 | 预登记 | 读法（09-16 写死） |
|---|---|---|---|---|
| M7R 对 M7（只改参考模型） | 3 | M7 2 : M7R 1 | M7R ≥55% | 未命中；决定票 3，只记不判。**票平不能说参考模型无效**——但 83% 都不要说明两首都不到你的线，参考模型对错在这个水平上看不出来 |
| M7R 对 M4 | 7 | M4 5 : M7R 2 | M7R ≥50% | 未命中；决定票 7，只记不判 |

篇幅：门控把 M7 的短诗和 M7R 的长诗都切掉了，上卷 M7 中位 143 字、M7R 153、M4 171、真人 173；篇幅比全在 1.6 以内，长的赢 4 : 短的赢 6——**长短不是这卷的变量**。

## 四、真伪题（附注）

| 臂 | 局 | AI 得票 | 真人 | 都不要 |
|---|---|---|---|---|
| M7R | 12 | 0 | 11 | 1 |
| M7 | 6 | 1 | 5 | 0 |
| M4 | 6 | 0 | 6 | 0 |

M4 输的两局有夸：《卡夫卡致菲丽丝（2）》对张枣「左边还不错」（左边是 M4）、《怀着一个欲念……》对马雁「用词还不错」（指 M4）。M7R 的 11 局真人全赢，没有一句评语。

## 五、口味维度与诗人

十维无一显著（有你 12 : 5，p=0.14）。无真人对真人局。批注 3 条：M4 「量词滥用，但是遣词造句还可以」（《西行吊古》，仍选了 M4）。

## 六、你的票怎么进材料

3 条批注入 `all_marks.json`；票入 `labels_owner_v13.json`；真伪 23 局决定票产 `dpo_rows_owner_v13.json`（正例 22 首真人 + 1 首 M7，真人不做正例，只供反例侧）；机机决定票 10 局：M4 胜 5、M7 胜 2、M7R 胜 3；23 局都不要两侧可做反例。总账更新到 `arm_ledger_v5_v14.json`。

## 七、这卷改写了什么判断

1. **参考模型接对不是灵药。** 09-05 归因、09-16 剂量实验都证明「参考=裸底座」把诗拽短拽碎，M7R 把它接对之后诗确实不短不碎了（面板碎行 14% → 4.5%、短诗 52 → 13）——你照样 83% 都不要。短碎是症状不是病。
2. 与 v12 一致：pt9 这一代不论怎么做偏好训练，在你眼里都在线下。M7R ≈ 桥 sft_pt9（几乎没训），所以这卷实际测的是「桥 sft_pt9 对 M4」= 2 : 5。
3. 预登记 ≥55% / ≥50% 在决定票 3 和 7 上没有检验力——09-16 已写明；这卷的信息全在都不要率里。

## 八、口径

`scripts/quiz/analyze_v13.py`（57 题 = ha 24 / aa 33；旧标题「硬通货」保留原样，本报告按新口径重排）。
