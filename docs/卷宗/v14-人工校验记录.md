# v14 制卷记录（2026-09-22，重训卷）

## 一、这卷是什么

主人 09-21 令：按溃败反思重训一版，先把分布问题归因修好，再出卷。本卷 80 题：ha 24 + aa 56，无 hh。

| 题型 | 题数 | 在问什么 |
|---|---|---|
| ha 真伪题 | 24 | M8 16 局、M4 8 局；真人在 A 侧 12 / B 侧 12 |
| aa 机机对决 | 56 | M8 对 M4 36 局（重训臂对现役）；M8w 对 M8sft 20 局（偏好训练的工作点对它自己的桥） |

四臂：
- **M8sft** = 桥 `outputs/sft_pt9b`（bf16 训的、上一代验证过循环 0.5–3% 的桥；本卷候选用 nf4 推理）。
- **M8** = 桥合并成完整权重 `outputs/merged_sft_pt9b`，其上做参考=桥的偏好训练 `outputs/dpo_m8b`（数据 cyberpoet_dpo_v6，3 轮 135 步，每 9 步存档），按训前写死的规则选 **checkpoint-45**（验证损失最低 0.6496）。
- **M8w** = 同一次训练的 **checkpoint-90**：ck45 的训练奖励差只有 0.17（<1.0 触发训前写好的备用条款），加一条「奖励差最接近 2.0」的工作点臂（ck90 奖励差 1.97）。
- **M4** = 现役（bf16 合并版 `outputs/merged_dpo_pt6` 推理，参数与三臂逐项相同）。

原计划的 sft_m8 新桥（量化训练）循环 10.6%，主人令先归因：底座/数据/训练量/退火全部排除，病因收窄到「量化训练或 8 条一批」（归因报告 09-21 更正）。新桥弃用，M8 改道到上一代已验证的桥，只换偏好训练。

## 二、头条口径（作答前锁定）

主人 09-21：「AI 诗至今也没有真正反超人类平均水平的，所谓反杀都是对手不是很强的时候。」Codex 同日评审同判。从本卷起：

- **头条只报「都不要率」**（每臂在它出场的每一局里主人选「都不要」的比例，ha 局与 aa 局分开）。v9–v11 全卷都不要率 25% / 55% / 52%，先看这一行有没有往下走。
- **对强真人诗（标准局）本卷无**：本卷真人诗都是首次上卷，主人还没在纯人对里评过它们；v15 起先评一批真人诗再设标准局。
- 反杀率、臂间胜率降为附注，不再叫「硬通货」；过关诗册停更。

预登记（写死在 `analyze_v14.py`）：M8 对 M4 >55%；M8w 对 M8sft >55%；45–55% 记均衡，<45% 记负；决定票 <30 只记不判。

## 三、出厂检

| 项目 | M8sft（桥） | M8（ck45） | M8w（ck90） |
|---|---|---|---|
| 面板 198 首，rp=1.08：字数中位 | 226 | 262 | 338 |
| 循环（同行 ≥3 次） | 0.5% | 2.5% | 0.5% |
| 碎行率 / <60 字 | 0.5% / 5 | 0.0% / 2 | 0.0% / 1 |
| 撞顶（≥555 token） | 5.6% | 9.6% | 15.7% |
| 关重复罚（rp=1.0，48 题）循环 | 69% | 52% | — |
| 记忆核验（面板，对 pt9_s1） | 0 首含重合窗 | 0 首 | 6 首，最高 11 窗 |

偏好训练曲线（验证损失 / 训练奖励差）：9 步 0.695 / −0.02 → 45 步 **0.650** / 0.17 → 90 步 0.698 / 1.97 → 135 步 0.794 / 3.09。开局损失 0.692、开局奖励 ≈0，参考=桥成立。日志见 `Quantizing model to 4 bit with bitsandbytes.`——这次是真量化（此前 93 份配置写 `bitsandbytes`、库只认 `bnb`，从未量化过；启动器已加硬闸）。

验收线（桥：循环 ≤5%、字数中位 120–300）四臂都过。关重复罚复读率仍高（52–69%），与历代同级，主人 09-08 已知此事仍裁决出卷；本卷不再拿它做门。

## 四、素材

`assemble_v14_sources.py`：ha 金标 40、aa 命题 86，种子 140002。**第一次抽样污染**：38/40 金标、71/84 命题在 v4 训练集里；重抽时排除 v4∪v5 全部训练题目与 10 字窗，aa 命题池扩到译诗人的题目（只用题目）。最终 126 题与训练题目 0 重合。
金标通读（`v14_gold_verdicts.json`）只做客观否决 8 首：拉丁字母题 4、题中带「致…」题献 1、组诗副题 1、全角空格排版页面无法呈现 1、末行疑似截断 1；aa 命题弃 10。

上卷真人：冯至 3、卞之琳 3、洛夫 2、多多 2、穆旦 2、海子 2、商禽 2、昌耀 2、陆忆敏 2、戈麦 1、张曙光 1、臧棣 1、痖弦 1。

## 五、候选与门控

四臂各 378 首（126 题 × 3 种子，种子 1400001–3；同题表、温度 0.9、top_p 0.9、重复罚 1.08、上限 560）。M8 一度 392 行（主线与侧线并发多写 14 首），按（题, 种子）去重到 378，原件存 `v14_cands_M8_raw392.jsonl`。

| 臂 | 过硬门控 | 落闸原因 | 过闸题数（/126） |
|---|---|---|---|
| M8 | 207/378 | 长于 340 字 142、短于 60 字 7、LIZHI 18、退化重复行 4 | 114 |
| M8sft | 228/378 | 长于 340 字 130、LIZHI 20 | 119 |
| M4 | 223/378 | 长于 340 字 92、短于 60 字 23、指令回显 15、LIZHI 10、退化 9、Latin 6 | 112 |
| M8w | 164/378 | **长于 340 字 200**、短于 60 字 1、LIZHI 13 | 102 |

M8w 有 53% 候选因过长落闸，上卷的 M8w 是它最短的那一半——这是门控造成的选择偏差，交付时如实说。M4 是唯一还有指令回显与 Latin 的臂。

记忆核验（对 pt9_s1 全语料，10 字窗）：M8 候选含重合窗 6 首、最高 5 窗；M8sft 10 首、最高 5；M4 13 首、最高 5；**M8w 8 首、最高 20 窗**（《我和我的火车和你》种子 1400001，438 字已被长度门拦下，未上卷）。无上卷诗 ≥20 窗。

## 六、盲读

流程：`propose_v14_slots.py` 出不带臂名的审读稿 → `v14_toread.py` 只列会上卷的那几首（匿名编号）→ 我逐首读、只记客观否决 → 重装 → 读替补 → 直到待读 0。共读 153 首（首轮 136 + 替补 17），全程不见臂名，读完才对回。

否决 10 首（对回后：M4 4、M8 3、M8sft 2、M8w 1）：

| 类别 | 数 | 例（AI 生成） |
|---|---|---|
| 题献 / 点名真人 | 4 | 「献给张枣」（M8w）、「答郑愁予君」并伪引吉鸿昌（M4）、「赠北岛」「赠芒克」（M8）、「献给所有…监狱里的人」（M8sft） |
| 伪造出处 / 伪副题 | 3 | 「——伊沙」（M8）、首行伪副题（M4）、「——一个关于“爱”的隐喻」（M8sft） |
| 伪日期 | 2 | 「（1956年）」（M8）、末行「1958.2.13」（M4；清洗只剥「年月日」式，点式漏过） |
| 名句化用 | 1 | 化用王维「空山不见人」「独坐幽篁里」（M4） |

四臂都有题献癖，与 v11–v13 同。复沓、长排比、「以…之名」串（ha44）一律不否决——修辞不做门。

## 七、装配与出品

- ha 24（M8 16 / M4 8）+ aa 56（M8vsM4 36 / M8wvsM8sft 20），十维可对比对数 device 35、register 45、heat 44、person/ending/simile/abstract 29、density 15、punct 11、linelen 10。
- `smoke_v14.js` 五项冒烟通过（正常答完 / 码导入退出 / 错版码拒收 / 全跳过 / 空卷）。
- 合成结果码（种下 ha 真人 15 / AI 5 / 都不要 4，aa M8 13 / M4 9 / M8sft 8 / M8w 4）：`analyze_v14.py` 逐项复原，头条都不要率、预登记对账、金标行 20 条均正确；合成产物已删。
- `register_v14.py`：登记表 3172 → 3332（本卷 160 首），备份 `used_bodies_pre_v14.json`。
- 交付：`交付_20260922/诗味测验14_重训卷.html`（sha256 ff6af62a…）与 `v14_交付附件_漏斗与失败.md`（8cd94475…）。附件含每臂漏斗、六类文本模式占比（童谣词 M8 36% / M8sft 30% / M4 27% / M8w 31%；顺序记事 M8w 24%；量词串 M8sft 33%）、每臂前三类失败的 AI 原文短摘。

## 八、没做成的

- **M8n**（偏好数据去掉 v1 时代 183 对的消融臂，Codex 建议）：数据与配置就绪（`data/dpo_train_v6nov1.json` 174 对、`configs/dpo_m8n.yaml`），09-22 凌晨八张卡空余都在 21 GB 以下，没训。等卡训好后另出小卷或并入 v15，不补进本卷。
- 标准局（对主人夸过的真人诗）：要先请主人评一批真人诗，v15 起。

## 九、等主人

v14 结果码；v12、v13 结果码仍未收到。
