# 训练谱系、语料家底与下一阶段训练计划（2026-08-31 深夜，主人三问的正式答复）

## 一、M4 / M5 / M6 各是怎么训的（逐段配方）

| 臂 | 语料 | 预训练 | 退火 | SFT 桥 | DPO | 微调方式 |
|---|---|---|---|---|---|---|
| **M4**（现役） | pt6 158 万字（外译 25% 帽） | 7 轮 QLoRA r32 lr 5e-5 | 纯中文诗 1 轮 | 2 轮 lr 2e-5 | **dpo_v1**：290 行，全是主人在 AI 对 AI 里投的票 | QLoRA 4-bit |
| **M5** | ＝M4 的桥（sft_pt6） | （同 M4） | （同 M4） | （同 M4） | **dpo_v4**：484 行 = v1 290 + 194 行真人金标 chosen | 同 |
| **M6** | pt8 224 万字（散文 30%、译体 1/3 帽） | 7 轮 QLoRA r32 | pt8 自带纯中文诗子集 | 同 | dpo_v4 | 同 |
| **M6b**（今晚新） | pt8 全链同 M6 | 同 M6 | 同 M6 | 同 M6 | **dpo_v1** | 同 |
| **M5b**（今晚新） | ＝M4 的桥 | — | — | — | **dpo_v5**：389 行 = v1 290 + v7/v8/v9 全部 aa 决票 | 同 |

M5 与 M4 只差 DPO 数据；M6 与 M5 只差语料代际；M6b 与 M4 只差语料代际（DPO 相同）；M5b 与 M4 只差 DPO 数据。
v9 的结论：dpo_v4 有害（M5 对 M4 3:10），所以 M6 与 M4 打平不能读成「新语料没用」——M6b 才能单独回答这一点。

## 二、全参数训练：尸检之后开过修复版吗？——没有，今晚开

08-29 尸检确证旧全参配置等于「没训练」（79.6% 参数逐比特未变）之后，我把全参改为「暂缓」，
定了重开触发条件（LoRA 容量瓶颈信号）。**这是我的判断保守了**：修复版模板（`fullparam_fixed.yaml`：
杠杆是切块间隔与学习率，删 pure_bf16 是空操作）从未被实跑验证，「LoRA 对全参」这个问题至今没有一次干净的检验。
今晚补上：**M7F** 与 **M7** 用完全相同的三阶段语料、轮数与后段，只差微调方式；且按强制规矩，
M7F 在第 50 步就做权重差分自检（>50% 参数未变即熔断），段末再查一次。

## 三、语料家底：手上有多少，训进去多少

| 类别 | 洗净可用（非空白字） | pt8 用了 | 未用 | 说明 |
|---|---|---|---|---|
| 中文原创诗 | 约 1.27 M（base 63 万 + poets_v2 75 万，去重后） | 1.12 M | ≈0 | 差额 = dev/test 隔离 664 首 + 变体去重 |
| 译诗（含新册 11 万） | 0.52 M | 0.46 M | 0.06 M | 单户 15% 剔 6 条 + 隔离 |
| 中文散文（马雁/张枣） | 0.47 M | 0.39 M | 0.07 M | 散文帽 |
| **译体散文（布罗茨基/曼氏/米沃什/策兰）** | **0.78 M** | **0.27 M** | **0.52 M** | **译体 1/3 帽把三分之二挡在门外** |
| 顾城 | 289 首 | 22 首 | 267 首 | 主人手挑，不算浪费 |
| 野草 | 2 万 | 0 | 2 万 | 待繁转简（本机无 opencc），本轮不入 |
| **合计** | **≈3.04 M** | **2.24 M** | **≈0.6 M** | 未用的 87% 是译体散文 |

**结论**：之前的「帽」是为一锅煮设计的风险控制；要不浪费语料，就把帽换成「阶段」。

## 四、下一阶段训练计划（已深思，已点火）

### 三阶段配方（pt9，`assemble_pt9_stages.py`，无任何配比帽，隔离与去重与 pt8 同）

| 阶段 | 内容 | 字数 | 轮数 | 学习率 | 为什么 |
|---|---|---|---|---|---|
| 1 见世面 | 全部：中文诗+译诗+中文散文+**全部译体散文** | 266 万 | 2 | 5e-5 | 想法最好、语感最洋的料放最远端，先吸收「文学怎么思考」 |
| 2 回中文 | 中文诗 + 中文散文 | 152 万 | 3 | 5e-5 | 把语感掰回中文；散文教转折与压力分配 |
| 3 定调子 | 纯中文诗按权重面板加权（含顾城手挑）+ 10% 回放 | 127 万 | 1 | 2e-5 | 末段决定味道（M4b 消融已证）；回放防遗忘 |

后段不变：SFT 桥 2 轮 → **DPO 用 dpo_v1**（唯一被证明有效的偏好数据）。

### 两条链，一个单变量阶梯

- **M7** = 三阶段 QLoRA。对 M6b（同 dpo_v1）= 「分阶段 + 全量译体散文」的净效应。
- **M7F** = 阶段一、二用修正版全参（BAdam interval 4，lr 3e-5，pure_bf16 保留），阶段三及后段 LoRA。
  对 M7 = 「全参 对 LoRA」的首次干净检验。
- 已在跑的 **M6b**（pt8 + dpo_v1）对 M4 = 纯语料代际；**M5b**（M4 桥 + dpo_v5）对 M4 = 偏好构造。

### 时间与出卷
- 21:00 M6b 完成；M5b 约 22:20 完成；6 号卡随后自动生成 M6b/M5b 出厂检面板与 **v10 三臂候选**（约 1 小时），
  再接 M7 三阶段（约 5.5 小时）；7 号卡 M7F 约 8 小时。
- **v10 卷（M6b / M5b / M4）今夜装配交付**；M7 / M7F 明晨出臂，出厂检后组 v11。

### 我否决过的备选（供审）
- 把 dpo_v4 的 194 行真人金标继续当 DPO chosen：v9 已判负，转作退火/SFT 材料（主人「都转」）。
- 解码加重复惩罚：撞「罚执行不罚特征」裁决。
- 只训 LoRA 不试全参：全参从未被干净检验过，今晚补。
- 全参做完三阶段：阶段三只有约 75 步，切块扫不完 40 层；故阶段三起改 LoRA，比较仍公平（两链只差阶段一、二的方式）。
