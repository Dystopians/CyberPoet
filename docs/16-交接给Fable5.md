# 交接 · 给 Fable 5 窗口

写于 2026-08-27 16:30，作者：Claude 窗口（负责清洗与文档）。
目的是让你一次看清所有悬而未决的问题，自己决定先做哪个。**不必等我。**

---

## 〇、铁律（先读这条，否则会重蹈今天的事故）

同目录的 `CLAUDE.md` 是强制约定，要点：

1. **数据目录写权独占**。`pw_crawl/` 是你的，`corpus_clean/` 是我的，
   `cyberpoet_v1/` 是 codex 的。别人只读。
2. **绝不原地改写**。清洗/转换一律读 A 写 B，不许写回输入文件。
   今天出过事故：审计脚本把结果写回了你正在写的 `audit_package/`，
   你一重跑，2753 条数据的审计标记全没了。
3. **不用模型评价文学性**（主人明令）。只统计可数的东西，原诗交给主人读。
4. **标注一律 2 选 1**，不做四选一。
5. 公开仓库不得含诗歌正文、训练数据、模型权重（语料是在世诗人版权作品）。

---

## 一、现在的状态

**语料已全部清洗完并接入训练，未启动训练。**

| 产物 | 内容 |
|---|---|
| `claude_night_20260827/corpus_clean/poets/` | 爬虫数据审计后 2488 首 / 29 位 |
| `corpus_clean/base_fixed/all.jsonl` | v0.3.0 基线，洛夫分节已修 |
| `corpus_clean/pt5_train.json` | **训练语料 v5：3734 首 / 971,029 字** |
| `configs/pt5_r32.yaml` | 训练配置，配方承袭 pt3_r32 |

三版语料对照（真损坏只数短行诗，散文诗不计）：

| 语料 | 首数 | 字数 | 真损坏 | 非中文 | HTML实体 |
|---|---:|---:|---:|---:|---:|
| pt2 | 1785 | 652,834 | 330 | 0 | 0 |
| pt3（此前在用） | 3162 | 953,666 | 331 | 2 | 11 |
| **pt5** | **3734** | **971,029** | **0** | **0** | **0** |

重建方式（你补完爬虫后直接跑，幂等，历史修复自动重新应用）：

```bash
cd /data/peilincai/CyberPoetTraining/claude_night_20260827
python3 clean_pipeline.py && python3 fix_base_corpus.py && python3 prepare_corpus_v5.py
```

---

## 二、待解决问题（按我判断的优先级，理由附上，决定权在你）

### P0 · 补爬洛夫 —— 语料质量上最大的一块缺口

v0.3.0 基线里洛夫 565 首中 **555 首是双倍行距损坏**（每行之间被插了空行，
原分节信息被彻底抹掉：全文件无「连续两空行」，真分节与假空行无法从文本自身区分）。

修复靠 PoemWiki 做对照源，但 PoemWiki 只爬到洛夫 186 首，所以：

- 146 首移植回了**真分节**（135 首正文逐字命中 + 11 首同题近似本对齐）
- **385 首只能压平，分节永久丢失**

**补爬洛夫后重跑管线会自动多救回一批。** 这是投入产出比最高的一件事。
洛夫的 PoemWiki id 在 `pw_crawl/authors.json`：`MTQxNTc1NzU3NTc1NTQ=`。

### P1 · 继续爬 —— 天花板在语料量

| | |
|---|---:|
| 已枚举诗人 | 4413 位 |
| 已爬 | 29 位 |
| 待爬 | 1882 位 |
| 现有语料 | 97 万字 |
| 路线图目标 | 700 万字（现 13.9%） |

`enum3.log` 16:14 仍在长，枚举还没完。爬到的新数据我这边幂等重跑即可，
不需要你做清洗——**但务必只写 `pw_crawl/`**。

### P2 · 训练是否用 pt5 开跑

pt5 已就绪。主人 08-27 说过「训练重跑等 Fable 5 窗口反馈」，所以我没擅自启动。

- 配置：`configs/pt5_r32.yaml`
- 启动：`cd cyberpoet_v1 && CUDA_VISIBLE_DEVICES=<空卡> llamafactory-cli train ../claude_night_20260827/configs/pt5_r32.yaml`
- 规矩：**有空卡立刻占用，不排队**；启动即钉 `CUDA_VISIBLE_DEVICES`；绝不与他人进程共卡。

**注意**：dev 集此前 221 首里有 54 首是受损洛夫，我一并修了。
所以 **pt5 的 eval loss 与 08-27 之前的 run 不可直接比较**。

权衡：现在跑（语料已比 pt3 干净得多）vs 等洛夫补完再跑（少浪费一次 9–10 GPU 小时）。

### P3 · M1 轮数落点 —— 数据支撑不了这个决定，建议直接收摊

主人 60 对标注昨晚 01:01 就交了（`vocab_probe/labels_m1_epochs.json`），
一直没人算。我今天算了：

| 档位 | 胜 | 场 | 胜率 |
|---|---:|---:|---:|
| M1_e4 | 17 | 30 | 57% |
| M1_e7 | 16 | 30 | 53% |
| M1_e5 | 15 | 30 | 50% |
| pt_sft | 12 | 30 | 40% |

两两对决全部不显著（每组仅 10 场，p ≥ 0.344）。要在 60% vs 50% 上做到
80% 把握，每组约需 194 场——即约 1200 对标注。

**建议**：别再为轮数做标注了，取 e4（最省且名义最高）收摊，
把标注预算全部投到 DPO 上。这一条我倾向很强，但请你判断。

### P4 · 三个审计判断，主人尚未裁决

1. **同诗异译是否算重复**——现保留。巴列霍《黑色的使者》有赵振江/陈黎/王央乐
   三个译本，是不同的中文文本，对语料有价值。
2. **首行与标题相同的 91 条**——现保留。`prepare_corpus_v4.py` 会删掉那一行，
   我判定为误伤（俄语首句作标题、周梦蝶《行到水穷处》等）。
3. **近重复的选留策略**——现按「汉字多者留」。已知一例反例：
   郑愁予《野店》留下的那份含 OCR 错字「黄昏裹」，正确的那份被剔。

这三条不阻塞任何事，可以带着跑。

### P5 · 磁盘

`/data` 97%，剩 1.1T。`claude_night_20260827/` 53G，`cyberpoet_v1/` 75G。
`outputs/pt2_full` 单独占 28G——全参臂已在主人盲测中 0/30 完败、路线已关闭，
可以整个删掉，但**那是 codex 的地盘，别替它删**。

---

## 三、目标口径（别跑偏）

主人的原话：

- 「不需要学诗人标志词，那明显是抄袭。用词的分布比较像就好」
- 「或者说，不那么像机器就够了」
- 「叠句必须得足够先锋我才喜欢，一般的堆叠会让我像反感垃圾比喻一样反感」
- 「不要用模型评价文学性。模型的审美品味普遍比较糟糕」

已关闭的路线（别回头）：14B 全参微调（盲测 0/30）、lm_head 扩展（无增益）、
AI 评委（曾把结论评反：base 在 62 场人工盲标里只赢 2 场）。

---

## 四、我的建议顺序

**P0 补洛夫 → P1 继续爬 → 重跑管线 → P2 开训**，P3 直接取 e4 收摊，P4 带着跑。

理由：现在最贵的资源是主人的标注时间和 GPU 小时，而语料量是唯一还能抬天花板的东西
（97 万字 / 700 万字目标 = 13.9%）。轮数调优已被证明在噪声里，不值得再投。

但这只是建议——你手上有爬虫的实际进度，自己判断。
