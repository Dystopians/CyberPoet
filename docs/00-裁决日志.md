# 裁决日志 · 只增不改

按时间倒序。每条记：**谁定的、依据什么、原话**。
**被推翻的判断与裁决同等重要**——它们标出哪些路已经走死，不要重走。

---

## 2026-08-26

### 主人裁决

**23:0x · M1 的落点待定**，60 对两选一标注页已交付。

**22:5x · 阅读包形式纠正。** 原话：「你没把我的M1阅读包出成题目吗？怎么给我的
真的是阅读题啊，这样我怎么反馈给你？」→ 凡需主人反馈的，一律出**可标注的页面**，
不出只读文档。

**20:2x · 题量加到 30**（原 12）。原话：「题多一点儿也没事。」

**19:5x · 加做 4 轮与 5 轮两个 SFT 落点**，避免只能在「7 轮过头」与「回到 2 轮」间二选一。

**18:2x · 提示词定精简版**（非谱系）。原话：「我的建议是精简。」
此裁决**覆盖**审核文档 §六「平局→取谱系」的预注册规则（主人 > 预写规则）。

**18:1x · 作者条件化停。** 原话：「一、停」。`pt_author` 训练在 7.04 轮中止、权重已删。

**16:5x · 目标口径重定。** 原话：「不需要学诗人标志词，我觉得有点儿走偏了，
那明显是抄袭。我觉得用词的分布比较像就好啊」「或者说，不那么像机器就够了」。
→ 标志词覆盖作废为判据；分布度量须排除人名/地名/专名。

**14:2x · 叠句的门槛。** 原话：「叠句必须得足够先锋我才喜欢，比如去你的xxx这种，
一般的堆叠会让我像反感垃圾比喻一样反感。」
→ 修正了「形式装置即优势」的过宽结论；实测强行首重复只赢 31 输 17（p=0.059）。

**14:0x · 分析深度要求。** 原话：「必须深入到用词层面。这其实是我最关心的部分之一。
如果模型没有学会像那些诗人一样用词，这个模型必然是失败的。」
→ 产出 `docs/10-偏好画像.md` 第七节（用词层面七小节）。

### 被数据推翻的判断（Claude 提出，实测否定）

| 判断 | 否定它的证据 |
|---|---|
| lm_head 未训练是词汇问题主因 | 加训 lm_head 全维度无收益、篇均字缩到 74；反例本在项目内（冻结读出层时硬套话已 0.90→0.14）。**14B 全量微调路线随之关闭** |
| 7 轮是过训练 | 7 轮 10 字窗重合全为 0，无背诵证据；该判断当初按 eval loss 定，方向错了 |
| 比喻越少越好 | 只在跨模型时成立；同模型采样中主人偏好比喻**更多**的一方（16:5，p=0.027） |
| 题面复读（「让…成为…」）会被讨厌 | 172 对里仅 7 首含该句式，选弃各半（p=1.0）；作为「都差」的预测因子也不成立 |
| 首行照抄题面会被讨厌 | 判双负率 38% vs 43%，无差别 |
| 重复惩罚压制了诗人式词汇复现 | 关掉后篇内重复率 0.194→0.407，远超真实诗人 0.106（退化为复读） |
| 陈述语域随预训练轮数单调上升 | 预训练阶段成立（16.7→55.4），**过了 SFT 桥后不成立**（4轮 0 / 5轮 332 / 7轮 207，最高是旧版 560） |
| packing:false 导致大量 padding | batch=1 无 padding；真正的低效是短序列吃不满卡 |
| 「机器口音词表」可跨题集使用 | 换到新题集后两个模型都是 0/万而真实诗人 22/万——该表是从旧场景题语境选出的 |

### 事故与教训

- **抢卡 OOM**：两个会话同时用 0 号卡。→ 启动即钉卡、启动前查占用。
- **裁决未同步**：codex 照着已被否决的 T3 多跑半小时。→ 裁决立刻写进 `STATE.md`。
- **checkpoint 被轮换删除**：`save_total_limit: 6` 把验证损失最优的
  `checkpoint-350` 删了。→ `save_only_model: true` + 保留档数覆盖预期最优点。
- **`pkill -f` 误杀自身 shell**：模式串匹配到了发起命令的 shell。→ 用 pid 精确 kill。
- **首次背诵命中**：旧版 pt_sft 在「以《我坐在一棵木头中》为题」复现海子原诗 **28 字**。
  → 诗题命题会诱发背诵，**终审须改用意象命题**。

---

## 2026-08-25

**主人裁决**

- **不用模型评价文学性**。原话：「不要用模型评价文学性。模型的审美品味普遍比较糟糕。」
- **标注保持两选一**，否决四选一。原话：「不要，请保持2选1。」
- **看到空卡立刻占**，不排 gpu_gate 队。
- **提交身份** `Dystopians <karamazovaniki@gmail.com>`，去掉 Co-Authored-By。
- **公开仓库不放诗歌正文**（版权在有效期内）；大文件走 Drive，不直接传输。

**关键发现**

- 基座在文学上完败：两批合计 **2 胜 68 负**（p = 4×10⁻¹⁸）。
- **AI 评审的判断是反的**：它称「基座 36 题盲评胜 31 场」，而主人给了基座 2/62。
  v1/v2 依据该评审被判不予晋级——**它们在文学上并未失败**，失败的是形式服从。
- 微调数据五处缺陷（指令自相矛盾 1521 条、形式栏 81.6% 造假、物象栏 73.1% 不出现、
  洛夫 501 首被电子书排版拆散、改写类草稿与答案无关）已修复并逐条复核。
