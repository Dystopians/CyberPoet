# 当前状态 · 唯一权威

**任何 agent（Claude / codex / 其他窗口）接手前先读完本文件。**
深文档（`docs/*`）只作查证；本文件与深文档冲突时，以本文件为准。
最后更新：2026-08-28 深夜（Fable 5 窗口，主人指示推送）。

---

## 一、目标口径（2026-08-26 项目主人重申）

> **不学诗人标志词——那是抄袭。要的是用词分布比较像；或者说，不那么像机器就够。**

唯一的终审是**项目主人本人盲读**。不用任何模型判文学质量、不排序、不投票。

---

## 二、当前有效的裁决

| 日期 | 裁决 | 依据 |
|---|---|---|
| 08-26 | **诗人标志词覆盖不再是判据**，作者条件化轴关闭 | 主人：「明显是抄袭」 |
| 08-26 | **系统提示词用精简版 v2**（`configs/poetry_system_v2.txt`） | 主人读 40 组对照后定；盲标 9:10 分不出 |
| 08-26 | **训练/推理措辞必须一致**：SFT 数据 system 字段同步换成 v2 | v2 时代的教训 |
| 08-26 | 标注**一律两选一 + ↓「都差」键**，不做四选一 | 主人否决四选一 |
| 08-26 | 机械分数**只可排序，不可当准入门** | 同模型采样上 quality_score 准确率 49% |
| 08-26 | 旧 36 道场景题**停用于一切新评估与偏好采样** | 题目自带剧本，把输出往一个方向赶 |
| 08-25 | 看到空卡**立刻占**（`CUDA_VISIBLE_DEVICES`），不排 gpu_gate 队 | 主人指示 |
| 08-25 | 提交身份 `Dystopians <karamazovaniki@gmail.com>`，**不加 Co-Authored-By** | 主人指示 |
| 08-25 | 主人**接受模型记住语料**，但 10 字窗重合数字每轮必报 | — |
| 08-27 | **盲测失利不推翻路线**；题组每对记录原作 tier 档 | 主人：对手原作质量参差 |
| 08-27 | 臂选择**永不用面板/机械指标代判**；题组混装多臂+隐藏标签由主人盲选裁决 | 全参栈 0/30 教训 |
| 08-27 | 口味配比：张枣全量；顾城 10%；口语系（韩东/于坚/伊沙/车前子/余怒/郑小琼）减半；朦胧派不再扩 | 主人裁示 |
| 08-27 | 译诗排除对白银-策兰-特翁谱系白名单**解除** | 主人点名外译四家 |
| 08-27 | **非诗歌文学批准受控掺入**：≤20%、P1 清单、散文在前末 1–2 轮纯诗退火、单变量对照 | 主人批准 |
| 08-27 | 轮数收摊取 e4；标注预算全转 DPO | 两两对决全不显著 |
| 08-27 | 数据流程：爬取→审计包→清洗 agent 剔不规范→语料构建只认审计后版本 | 主人定流程 |
| 08-27 | 测验规范固化：零重复登记表、逐对人工读、篇幅比≤1.6、侧位配平、五档判定（45–55%均衡不判）、维度覆盖自检、双倍行距/红旗门控 | 主人五点要求+三轮迭代 |
| 08-27 | 结果页不发明称谓；默认层=口味词象（选中诗的手摘原文词）+硬计数；诗人光谱入详细层 | 主人三条反馈 |
| 08-27 | v5 解盲：火眼 22/24；aa 两臂 6:6 不可分 → **撤 dpo_full vs M2_dpo 题型**，改 M3 对旧臂 | 主人卷 |
| 08-27 | 新强信号 **「有你」16:7**（第二人称对话感）；仅有的两首反杀 AI 诗均为亲密第二人称抒情，互证 | 主人卷交叉验证 |
| 08-27 | **语料纹理逐字传染实证**：M2 臂生成复现 pt3 损坏洛夫的双倍行距（3 首全为 M2）；AI 候选一律压平门控 | v5 事故复盘 |
| 08-28 | **测验永久双范式**：朋友版 60 题定比例（24/24/12）；主人标记版按信息密度设计、题量可 100+；主人版剔知名诗 | 主人裁决 |
| 08-28 | 题型职能定型：ha=DPO 主力（真人金标 chosen/自家生成 rejected）；aa=臂间对照；hh=扩充诗人语料加权投票 | 主人质询后重规划 |
| 08-28 | ha 候选须人工通读筛选，笑场级不放行 | 主人指示 |

### 已关闭的路线（不要回头）

~~14B 全量微调~~（**08-27 主人重新授权全参数微调**，已用单卡 BAdam 跑通，见 docs/13；当时关闭的是三卡 FSDP 方案，因依赖不可靠） · 形式服从专线 · 字数控制 · 词表裁剪 · 作者条件化 ·
机械质量门 · AI 评审文学 · 四选一标注 · 在 37 万字语料上加大 LoRA rank

---

## 三、此刻的状态（2026-08-28 深夜）

**在跑**：① M3 的 DPO（`configs/dpo_pt5.yaml`，卡 3）；② PoemWiki 疑似名单重探
（721/2567，服务端自适应限流，速率 214→64 条/时递减，剩约一天；**爬取已解耦，不阻塞**）。

**pt5 已训完**：8 epoch 干净收官（train 3.69 / eval 3.88，dev 修复后口径）。
逐 epoch 退化面板全过：分点 0%、行重复≤语料上限、对新旧语料**均零逐字记忆**；
抽读无洛夫式假分节。桥接取 e8。

**M3 谱系（单变量对照）**：pt5 → SFT 桥 v3（eval 2.95）→ DPO（与 M2 同数据同参）。
与 M2（pt3 基座）唯一差异 = 语料修复。出臂后先复查双倍行距是否随语料修复消失。

**测验体系**：v4（60 题定版流程）→ v5（60 题、双层结果页、主人已答）→
**v6 主人标记版 100 题在制**（24 hh + 52 ha + 24 aa；hh 已装配且 112 首逐字读完，
含 14 家扩充诗人参战与译配译约束；ha/aa 等 M3 出臂生成）。
主人 v5 卷：火眼 22/24；反杀 2 首入过关诗册（现 8 条对开）；
偏好行产出 `dpo_rows_owner_v5.json`（12 行）。

**爬取**：口味名单 52 目标**已全部爬完并入 pt5**；语料现 97.1 万字（目标 700 万的 13.9%）。
下一波增量 = 重探新发现（完成后出清单交主人裁口味归属）+ P1 非诗歌文学（等主人找书）。

**训练路线**：三臂对照已做完（LoRA r32 / r64 / BAdam 全参）。
**30 对鉴别题组解盲：全参臂 0/30 完败，LoRA 系为唯一过关谱系。**
全参虽然篇幅分布最贴合真实诗，但主人盲读判定不过关——**臂选择永不用面板代判**。

**洛夫分节修复**：已合入语料。555 首损坏中 146 首从 PoemWiki 移植回真分节，
385 首压平（分节永久丢失，除非补爬洛夫）。dev 集 221 首中 54 首同属受损，
一并修复——**eval loss 与 08-27 前的 run 不可比**。

**DPO**：首次跑通，主人历史盲标汇成 290 对，β=0.1，奖励准确率 75–82%。

**等主人（TODO，同步在 README）**：
1. **P1 非诗歌文学找书**（docs/16 清单：张枣/马雁随笔、布罗茨基《小于一》、
   曼德尔施塔姆、《野草》系散文诗等；任意格式丢服务器即可，管线现成）。
2. v6 主人标记版 100 题交付后作答。
3. 重探完成后裁决新发现诗人的口味归属清单。
4. 公开库 git 历史里仍存过一版含诗歌正文的 html（已删文件但历史可查）——
   是否 filter-repo 重写历史由主人定。
5. 三个数据审计裁决项（同诗异译/首行重复标题 91 条/近重复选留）——不阻塞。

**磁盘**：`/data` 97%，剩 1.1T。`claude_night_20260827/` 53 G、`cyberpoet_v1/` 75 G。
其中 `outputs/pt2_full` 占 28 G——全参路线已关闭，可删，但那是 codex 的地盘。

### 语料（08-27 下午重建）

- 清洗产物统一在 `claude_night_20260827/corpus_clean/`，由脚本幂等重建，勿手改。
- 训练语料 **v5：3734 首 / 971,029 字**（`corpus_clean/pt5_train.json`）。
- 洛夫双倍行距损坏已修：555 首中 146 首从 PoemWiki 移植回真分节，其余压平。
- 已接入训练：`cyberpoet_pt5_train` / `cyberpoet_pt5_dev`，配置 `configs/pt5_r32.yaml`。
- dev 集此前含 54 首受损洛夫，修复后 **eval loss 与 08-27 前的 run 不可比**。
- `prepare_corpus_v4.py` 已作废——它读未清洗数据、繁转简会转出生僻字，
  且 `pt4_train.json` 从未生成。改用 `prepare_corpus_v5.py`。

## 四、下一步（主人定完落点后）

1. T2 题目集切换已完成（`vocab_probe/probes_title.jsonl` 40 题 /
   `probes_imagery.jsonl` 40 题）。**终审须用意象命题那套**——
   诗题命题会诱发背诵（实测旧版复现海子原诗 28 字）。
2. T4 · DPO 采样：脚本 `vocab_probe/gen_dpo_bank.py`（codex 备），
   锦标赛配对、每题 8 采样、目标约 300 有效对（按都差率备 430 对）。
3. T6 · 生成提速：现 25 秒/首，DPO 一批 3000+ 首。上 vLLM 或至少 batch 16。
4. 语料扩建（唯一能抬天花板的一步）：现库 71 万字 → 目标 ≥700 万字。

---

## 五、并行作业的规矩

- **数据目录的写权是独占的**，清洗产物与爬虫产物绝不写在同一路径：
  | 目录 | 写权 |
  |---|---|
  | `claude_night_20260827/pw_crawl/` | 爬虫窗口（Fable 5） |
  | `claude_night_20260827/corpus_clean/` | 清洗窗口（Claude） |
  | `cyberpoet_v1/` | codex |
  | `cyberpoet_repo/` | Claude |
- **绝不原地改写**：清洗一律读 A 写 B，不许把结果写回输入文件。
  （08-27 事故：`audit_apply.py` 把审计结果写回爬虫目录，爬虫一重跑
  就冲掉了全部审计标记，2753 条白洗一遍。该脚本已废弃。）
- **清洗只走 `clean_pipeline.py`**，从原始数据幂等重建；爬虫出新数据直接重跑，
  历史修复规则自动重新应用。不要手工编辑 jsonl 修数据。
- 详见仓库外的 `CyberPoetTraining/CLAUDE.md`（各窗口自动加载）。
- **只加新文件，不改对方文件**；产物写明路径与 SHA。
- 启动即钉卡（`CUDA_VISIBLE_DEVICES`），启动前查占用。
  （08-26 事故：两个会话同时抢 0 号卡导致 OOM。）
- 裁决一旦下达，**立刻更新本文件**——今天 codex 多跑了半小时已作废的 T3，
  就是因为裁决没进权威文件。
