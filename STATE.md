# 当前状态 · 唯一权威

**最后更新：2026-09-23 00:45（Fable 5 总控窗口）。任何 agent 接手前先读完零、二、三节。**
权威部分是零节看板、二节现行规矩、三节结论表；六节是近期裁决摘要，全文在 [docs/卷宗/裁决日志_全文.md](docs/卷宗/裁决日志_全文.md)。与深文档冲突时以本文件为准。

**本文件的写法（四条）**：① 每次更新只做三件事——改顶部日期、改看板、加一条裁决（先在裁决日志全文加一行，再在本文件第六节加 ≤200 字摘要，同一次提交）；② 改一条结论必须同时改第三节与第六节；③ 「在跑什么」为空也要写「无」，写入前现场核 `nvidia-smi` 并区分本项目与他人进程；④ 数字一律带口径（重复罚、模板、dev 版本、题数、题面是否与训练重叠）。

---

## 零、一屏看板

### 0.1 现在信什么（等级：已验证 / 削弱后成立 / 待证；出处见第三节）

1. 六次偏好训练（DPO）的参考模型都接成了裸 Qwen3-14B，开局奖励 +8～+10 是既有差距不是学到的；约一半偏好对（47–49%，训练格式重算）开局就被判「已经赢了」——**已验证**（0.85）。
2. 三轮 DPO 没有背诵，只把反例压到底；正例打分不动——**已验证**（0.85）。
3. dpo_v1 按梯度 92% 以上是主人在 AI 对 AI 里投的票；31 行真人正例权重不到 2%——**已验证**（0.85）。
4. 关掉重复罚，SFT 桥 23/48 首整行复读，裸底座 0/48；复读从预训练第 1 轮就有——**已验证**（0.8）。
5. **拽短拽碎的主因是参考模型——已验证**（09-16 剂量实验）：参考正确时同一份 dpo_v1 训到奖励差 2–7，诗反而变长（283–340 字，短于 60 字 0–1/198），参考接错的臂 37 步就短 62 字。可用工作点在奖励差 2 左右（≥4 后写太长、撞顶三成）。M7R 本身几乎没训（≈桥）。
6. 主人的票：篇幅差两成以内，每行更长的那首赢 102 比 65（p=0.005）；差得大时长的赢 58 比 36；60–90 字的短碎病区没有票——**削弱**（0.72）。
7. 7 轮预训练没有依据，各链开发集第 2 轮前后见底；但开发集只算了 58%、pt6 的开发集含训练篇目，轮数判决待重算——**削弱**（0.78）。
8. 语料「质」的红利显著（p=0.008），「量」到 pt8 见顶；M5 线废止、pt8 代路线止——**已验证**（主人票）。
9. **09-21 之前所有标「QLoRA / 4 位量化」的训练其实都没量化，是 bf16 底座上的 LoRA**（配置里量化方式写成 `bitsandbytes`，训练库只认 `bnb`，写错不报错）——**已验证**（代码路径、47 份日志、显存 28–35 GB 对 12.9 GB）。以前的臂训练和推理都是 bf16，结论不受影响；M8 是第一次真量化训练。
10. **pt9 这一代（M7 / M7F / M7Fsft）整代不合格，偏好训练不是主病根**：v12 三臂反杀 0/40，机机都不要 77%，不做偏好训练的 M7Fsft 对做了的 M7F 21/24 都不要——**已验证**（主人票）。从 pt6 之后每一代新底座都没超过 M4，都不要率 25%→55%→52%→77%。M8 的桥就建在 pt9_s3 上，v14 是「病在底座还是病在桥/偏好训练」的判决卷。
11. **v13 + v14（09-22 解盲）：09-05 归因开出的三味药——参考模型接对（M7R、M8）、桥重建（sft_pt9b）、偏好数据换成主人票（v6）——在主人票上全部无效**：M7R 对 M7 1:2（83% 都不要）、M8 对 M4 **3:16**（p=0.004）、M8w 对 M8sft 4:6。pt9 底座上每个臂都输给 M4（M7F 0:10、M7R 2:5、M8 3:16）；短碎治好了票没动。**M4 同卷都不要率从 v7 的 5% 涨到 v10b–v14 的 47–58%（权重没变）→ 都不要率不是绝对刻度，只能同卷对着 M4 读**——已验证（主人票）。
12. **「语料扩大」不成立**（09-22 19:40 核查）：pt6 → pt8 → pt9 中文原创诗 4528 → 4501 → 4588 篇（3903 首相同，净增 122）；扩的是散文（pt9 第一阶段 41%、第二阶段 27%）与译诗。M4 每首诗读 8 遍，pt9 读 6 遍且 5 遍混着散文。同一座 M4 桥换最后一步数据即从最好变最差（M4 → M5）——训法能毁好底子。底座与训法分不开——**已验证**（数据核查 + 主人票）。

### 0.2 在跑什么（09-22 03:30 现场核）

| 任务 | 卡 | 起于 | 预计 | 产物 |
|---|---|---|---|---|
| **v15 已交付**（09-23 00:40，`交付_20260923/`：108 题 = hh 24 + ha 24 + aa 60；盲读 173 首否决 18，其中桥本身 12、M4 5、M4R 1）。实验 1 全链完。| — | — | — | `quiz_v3/v15_pairs_final.json` |
| （已完）**实验 1「M4R」链**：dpo_m4r **21:20 训完**（卡 7，44 分钟，135 步）：验证损失 0.696 → 最低 0.585（第 99 步）→ 末档 0.673；训练奖励差末档 3.6。对照 M8b（pt9 底座、同数据）：验证损失第 45 步 0.650 见底后升到 0.79——**主人的票在 M4 底座上学得进去、在 pt9 底座上学不进去**。按主人批准的规则不按验证损失挑：M4R = 第 135 步（末档），M4Rw = 第 72 步（奖励差 1.79）。**面板（22:10）**：M4R 中位 214 / 循环 1.0% / 碎行 0.5% / 撞顶 5.6% / 比喻 0.29；M4Rw 154 / 2.5% / 4.5% / 1.5%；桥 M4sft 156 / 5.1% / 4.0% / 3.0% / 比喻 0.50——偏好训练把诗训长、循环压下、比喻减半；末档不换。记忆核验最高 M4sft 面板一首 55 窗（未上卷）。现在出关重复罚诊断与 v15 候选。原计划：① sft_pt6 合并 → merged_sft_pt6（19:58 完，28 GB）→ ② 等卡 → dpo_m4r（与 dpo_m8b 同配置只换底座，nf4）→ ③ 末档 = M4R、奖励差≈2 = M4Rw → ④ 面板三臂（M4R / M4Rw / M4sft）+ 关重复罚 → ⑤ v15 候选三臂（题表 `titles_v15.json` 134 题已就绪）。随后 `run_m4_v15cands.sh` 出 M4 候选（bf16 合并版）。v15 制卷脚本已派生并用假候选全流程空跑通过（含合成结果码；抓到两处版本字面量 14）。 | 等卡 | 19:55 | 训练 1 h + 生成约 3 h | `outputs/dpo_m4r`、`quiz_v3/v15_cands_*.jsonl` |
| **实验 2「pt6+散文」链 · 23:47 已在 1 号卡开跑**（`run_pt6p_direct.sh`，整卡 48.5 GB 空出直接上；第一段 pt6p_r32 约 5 小时，后续段占位协议）。原设计：M4 配方一字不改（pt6 7 轮 → 退火 → sft_v3 桥 → dpo_v1 参考=裸底座，模板 qwen3_nothink，bf16 LoRA），只把 pt9 的 301 篇散文（123 万字）加进预训练（`data/pt6p_train_full.json` 6470 篇 291 万字）。等实验 1 生成完再抢卡；bf16 要 ≥34 GB 空卡，占到不够就放。估 5 + 0.3 + 1 + 0.3 小时 + 面板。 | 等 ≥34 GB 的卡 | 20:02 | ~7 h 训练 | `outputs/pt6p_r32 → pt6p_anneal → sft_pt6p → dpo_pt6p`、`quiz_v3/M4P_panel_gens.jsonl` |
| （旧行）M8n 消融臂 09-22 05:59 自行跑完（卡 6 空出时占到）：dpo_m8n 63 步、存档选中第 9 步（验证损失 0.678 并列取早，≈桥）、面板循环 1.5%、候选 378 首；未上卷。旧记录：M8b 链 09-22 01:46 跑完（dpo_m8b 135 步、选 ck45、备用臂 ck90、面板、候选、复读诊断、记忆核验）；M4 候选 03:04 完（bf16 合并版分两卡）。**v14 已交付**（`交付_20260922/`）。M8n 消融臂等一张空余 ≥21 GB 的卡（此刻八张卡空余 4.5–20.6 GB） | — | — | — | — |
| （旧行，留档）**M8b 链**（09-21 22:00）：偏好训练 dpo_m8b（底座 = 合并后的 sft_pt9b，nf4 加载；参考=桥；v6 数据；每 9 步验证）→ 按训前规则选存档 → 面板与 v14 候选（M8、M8sft = sft_pt9b）；M4 候选（bf16）另等一张 ≥31 GB 的空卡 | 四张卡此刻全满，预热占位挂在 3/4/6 号卡等空档 | 09-21 21:51 | 偏好训练 1 h + 生成 1.5 h + 盲读装卷 1.5 h，全看何时有卡 | `outputs/dpo_m8b`、`quiz_v3/v14_cands_*.jsonl`；执行器 `run_m8b_post.sh`、`run_m8_m4cands_v3.sh` |

八张卡上都有他人进程（各占 38–45 GB），只有 6 号卡空 30 GB；6 号卡上本项目 1 个训练进程，其余全是他人的。bf16 的 LoRA 要 ≥32 GB，现在没有任何一张卡放得下，所以 M8 用真 4 位量化训。

### 0.3 等主人拍板（编号与归因报告第 2 节一致）

1. 复读率出卷门槛改口径：198 首面板、开重复罚 1.08、循环 >5% 不出卷；关重复罚只做诊断；测题不得在桥的训练集里。默认：新桥 sft_pt9b 不凭现有数字放行。
2. 三个补充实验已全部跑完，结果已补进报告；A.2 建议改为「以奖励差 ≈2 为目标」（见报告第 7 节），等你点头。
3. 09-08 已批准的五条做法复核后要改口（A.2 / B.5 / C.9 / E.13 / E.14，见报告第 7 节）。默认：维持 09-08 文本，标「依据已削弱」。
4. 预训练轮数：先按篇全量重算开发集再定；此前不按 2–3 轮硬砍。
5. ~~v13 读票边界~~ 已按写死的读法读完（v13 解盲报告）：票负 + 83% 都不要，参考模型对错在这个水平上看不出来。
6. v11–v14 已全部解盲；**v15 已于 09-23 00:40 交付，等结果码**。
7. git 历史：三份 JSON 从未入库（误报已更正）；唯一问题是 0101729 的阅读包，是否重写历史。
8. v14 起制卷三处改动：读稿否决盲化、装配组内随机取候选、门控改报告篇幅差别并加篇幅比 2–3 的机机对。
9. 全参：先试 40 分钟「学习率 6e-5 或切块间隔 8」再决定 fp32 主权重对照。
10. dpo_v6 建对三条新规：留 40 对验证集；剔矛盾对与语料诗；dpo_v1 里 4 局真人诗做反例请确认本意。
11. M8 是被显存挤出来才用的真 4 位量化训练（不是改进项）：开发集损失比 bf16 高 0.024（0.6%，在量法灵敏度内）；M8 各臂按训练时的精度（4 位量化）生成，M4 照旧 bf16。要不要等空卡再训一版 bf16 的 M8 做对照（约 5 小时），你定。
12. **已批准（09-22 19:40「都没问题」）**，执行口径见 0.4：原文——① 「M4R」= M4 的桥 sft_pt6 合并后做参考=桥、v6 数据的偏好训练（与 M8 同一套修法），对 M4 出卷——单变量分开「偏好训练修法」与「底座」（3 小时 GPU）；② 底座实验：回到 pt6 语料配方，只加 22 家核准扩爬里清洗干净的诗（不加散文、译诗），预训练 2 轮出桥（几十 GPU 小时）。默认：有卡先跑 ①。
13. 制卷改口（v15 起，**已批准**）：每卷必带 M4 局做尺子，头条报「同卷对 M4」；标准局用你在纯人对里夸过的真人诗（陆忆敏 9/14、杨炼 10/12 是现成候选）。单首「要/不要」题型：主人未单独表态，v15 先不加。

### 0.4 下一步（按依赖）

1. **v15 结果码到 → `analyze_v15.py`**（头条同卷对 M4；hh 赢的诗 → v16 标准局）。实验 1 已完：M4 的桥 sft_pt6 合并 → 参考=桥、v6 数据的偏好训练，**不按验证损失挑存档**：固定 3 轮（与 dpo_pt6 同）另存奖励差 ≈2 的工作点 → 面板 → 候选。v15 三臂：桥本身 / M4 / 桥+新法偏好训练（+ M4 尺子局、标准局）。
2. pt9 底座上不再开新训练（M8n 已跑完，不出卷）。
3. **实验 2（已批，改口）**：不是换诗集——中文诗三代几乎相同（4528/4501/4588 篇）；改为「M4 配方一字不改（pt6 7 轮 + 退火 + sft_v3 桥 + dpo_v1），只把 pt9 的散文加进预训练」，直接检验散文是不是病因。约 12 GPU 小时预训练 + 2 小时后段；等卡。
4. 开发集按篇重算（GPU 1.5 h）→ 定轮数（0.3 第 4 项）。
5. bf16 的 M8 对照（0.3 第 11 项）——M8 已判负，建议撤销。

---

## 一、目标口径（2026-08-26 项目主人重申）

> **不学诗人标志词——那是抄袭。要的是用词分布比较像；或者说，不那么像机器就够。**

唯一的终审是**项目主人本人盲读**。不用任何模型判文学质量、不排序、不投票。

---

## 二、现行规矩（按主题；括号里是来历；「待裁」= 依据经 09-16 复核削弱，等主人）

**2.1 评审与终审**
- 模型永不评文学性（AI 评审曾判反，08-25）；可数指标只排序不做门（08-26）；臂间只由主人票裁决（夜训 0/30 教训，08-27）。
- 预登记写在开卷前、事后不改（08-28）；n<30 的命中/未命中只记不判、45–55% 记均衡（09-16 复核：≥55% 在 18 局无差时也有 41% 命中）。
- 「都不要」不入正例（08-31）；单卷信号必须跨卷复验（「有你」塌缩，08-28）。

**2.2 语料与配比**
- 权重面板在 README，主人改表即指令（08-28）；散文帽阶段制（08-31）；外译 25%、单户 15%、同诗异译诗人译本优先（08-27/29）。
- 不规避名篇（08-31）；极著名诗题不做 AI 命题、记忆核验 ≥20 窗否决（09-02）。
- 只加新文件不改别人的；不原地改写；清洗只走管线（08-27 事故）。

**2.3 训练配方**
- DPO 参考模型 = 合并后的桥（`merge_bridge.py`；09-05 归因、09-08 批准；第 5 步奖励 ≈ 0、损失 ≈ 0.693）。
- 正例只用当代模型自身生成，真人诗不做正例（08-31/09-02）；建对查字数/行数平衡；反例剥 Markdown（09-08）。
- 轮数按开发集最低点、保留逐轮存档（09-08）——**待裁**：开发集先修口径（只算 58%、pt6 含 dev 篇目）再定。
- 桥题面含「以《X》为题」、诗人全覆盖单家 ≤15%（09-08，新桥已做到）。
- 训练与推理模板逐 token 一致：用带空 think 块的 qwen3 模板 + enable_thinking=false，生成脚本不得改回（09-08/09-16）。
- 量化方式一律写 `quantization_method: bnb`；起跑后日志里必须有 `Quantizing model to 4 bit with bitsandbytes` 一行，没有就停（执行器已内置）；每个臂在它训练时的精度下推理（09-21）。
- **待裁**：奖励差正常区间（剂量实验后定）；剔底座反例（可留可删）；退火段学习率须低于主训；DPO 留验证集。

**2.4 出厂检**：一律按 [docs/现行/出厂检清单.md](docs/现行/出厂检清单.md)——提示一致性、权重差分（09-02 口径：零变化率 >70% **且** ‖Δ‖/‖W‖ <5e-4 才是未训签名）、参考模型核查、训前打分平均权重、SFT 段对照面板、复读率（口径待裁）、记忆核验、面板题面不与训练题重叠、面板数字标口径。

**2.5 制卷**：按 [docs/卷宗/README.md](docs/卷宗/README.md) 制卷规范。已知四条局限（09-16）：门控切掉两臂长度差；预登记检验力不足；质感排序实际决定 85–90% 上卷诗；读稿否决不盲。v13 两种结果的读法已写死在卷宗 README。

**2.6 并行作业与 GPU**
- 目录写权独占：`pw_crawl/` 爬虫窗口、`corpus_clean/` 清洗窗口、`cyberpoet_v1/` codex、`cyberpoet_repo/` Claude。
- 有空卡立刻占，启动即钉 `CUDA_VISIBLE_DEVICES`，钉前钉后各查一次（抢到卡 ≠ 拿到显存，08-28）；空卡判据已用 <2 GB 照占；bf16 的 LoRA 要 28–35 GB，真 4 位量化约 13 GB（09-21 实测 12.9 GB，已关掉 transformers 的显存预热），共卡时留余量（09-08 四次加载 OOM）。
- 提交身份 `Dystopians <karamazovaniki@gmail.com>`，不加 Co-Authored-By（08-25）。
- 裁决一旦下达立刻更新本文件（先全文日志、再摘要）。
- 公开仓库不得含诗歌正文、训练数据、权重；`.gitignore` 已挡 json/jsonl/权重/日志。

---

## 三、现在有效的结论

| 结论 | 等级 | 关键数字 | 混杂 / 测量范围 | 出处 |
|---|---|---|---|---|
| 微调方向本身有效 | 已验证 | 裸底座对一切微调臂 2 胜 70 负 | — | 证据链 00 |
| 语料质 > 量 | 已验证 | 修复损坏语料 22:7（p=0.008）；翻倍 14:8；pt8 代 8:13 | — | 卷宗 v6/v7/v10b |
| 装置人机反转 | 已验证 | 含机对无装置 126:67（p<0.001，四卷连显） | 唯一过多重比较的口味维度 | 标记总报告 0921 |
| 质感 = 诗级合格线 | 已验证 | 样本外 p=0.002 | 诗人级失效 | 证据链 07 |
| 主人长度口味 | 削弱 | 行更长的赢 102:65（p=0.005） | 只在篇幅比 ≤1.6 内测过 | 归因报告 4.2 |
| DPO 参考模型 = 裸底座 | 已验证 | 开局奖励 +8～+10；参考改对后 0.003 | — | 归因报告 4.1 |
| 开局差距远离 0 | 已验证 | 47–49% 的对梯度 ≈0；平均权重 0.32；加权后正例短 48–62 字 | 训练格式重算（09-16） | 归因报告 4.1、附录 A |
| 拽短的主因是参考模型 | 已验证（干预 + 剂量） | 参考正确训到奖励差 2–7 反而变长 +76～+167 字；参考接错 37 步短 62 字 | 具体机制（往底座退）仍是候选 | 归因报告 5、附录 E |
| 三轮 DPO 只压反例、不背诵 | 已验证 | 正例每字 −2.6 不动；权重变 1.6% | DPO 无验证集 | 归因报告 4.1 |
| dpo_v1 构成 | 已验证 | 按梯度 92% 以上 AI 对 AI 票；31 行真人正例 | — | 归因报告 4.2 |
| 7 轮无依据；dev 第 2 轮前后见底 | 削弱 | 22:10 p=0.0501；pt6 +0.068、pt8 +0.196 | dev 只算 58%；pt6 含 dev 篇目 | 归因报告 4.3 |
| 复读来自预训练 | 已验证 | 第 1 轮 9/48 → 第 3 轮 20/48；裸底座 0 | 48 题只分辨 20 个百分点 | 归因报告 4.3 |
| 模板失配机制 | 已验证 | 新桥去掉空 think 块首字损失 30.8 | 标点差不显著 | 归因报告 4.4 |
| 此前从未量化训练过 | 已验证（09-21） | 47 份训练日志无一出现量化那行；显存 28–35 GB 对真量化 12.9 GB | 「nf4 训 bf16 推无害」一行撤回：这种失配没发生过；实验 C 的 +0.10 是「bf16 训的 adapter 挂量化底座」的代价 | 归因报告 0（09-21 更正）、附录 C |
| 真 4 位量化的代价（预训练开发集） | 待证（单次） | pt10_s3 第 20 步 3.873，对 bf16 同存档约 3.849，高 0.024 | 量法灵敏度 ±0.04；文学性影响未知 | 裁决日志 09-21 |
| 全参 bf16 写回机制 | 已验证 | \|w\|≥0.031 零变化 100% | dev 差归因待证 | 归因报告 4.5 |
| M5 线废止；pt8 代路线止 | 已验证（主人裁决） | M5b 九成垃圾；M6b 8:13、都不要 55% | pt8 主因降为推断 | 卷宗 v10b |

**撤回的结论**：nf4 训 bf16 推无害（09-21：从未量化过） · 训到背诵 · 教的是别像裸底座 · 桥越强筛选越狠 · pt8 过拟合致「都不要」55% · 标点差三分之一 · 轮到次数少 · dev 差是舍入后果 · 训练长度信号与口味相反 · 「敢抽象」「少比喻」「有你」（多次塌缩）。

---

## 四、活的谱系

| 臂 | 配方一句话 | 参考模型 | 状态 | 主人票 |
|---|---|---|---|---|
| M4 | pt6 7 轮 → 退火 → sft_pt6 → dpo_v1 | 裸底座 | **现役**（v13 5:2 胜 M7R、v14 16:3 胜 M8） | 累计 AI 对 AI 104/150 = 69%；反杀 15/82 = 18% |
| M7 | pt9 三阶段 LoRA（bf16 底座；标的「QLoRA」09-21 查明未量化）→ sft_pt9 → dpo_v1 | 裸底座 | **v12 判负**（反杀 0/14，对 M7F 5:3 只记，都不要 67%）；待 v13 | 0/10 |
| M7R | 同 M7 的桥，参考改桥（几乎没训，≈桥） | 桥 | **v13 判负**：对 M7 1:2（83% 都不要）、对 M4 2:5、真伪 0/11 | 3/10 |
| 剂量臂 | 同 M7R，学习率 4 倍、5 轮（第 2 轮存档 = 奖励差 2.0） | 桥 | 对照实验，未出卷 | — |
| M7F / M7Fsft | 修正版全参 s1–s2 + LoRA；后者无 DPO | pt9F_s2 | **两臂都判负**（v11：M7F 对 M4 0:10、对 M6b 2:11；v12：M7Fsft 对 M7F 21/24 都不要、三臂反杀 0/40）——做不做偏好训练都一样烂 | M7F 0/10、0/13；M7Fsft 1/3 |
| sft_pt9b | 新桥：47 家、标题题面 47%、模板一致、按 dev 早停 | — | 待建对；复读率 2% 在训练题上测，未验证 | — |
| M8 / M8w / M8sft | 桥 = 上一代 sft_pt9b（bf16 训的；新桥 sft_m8 因循环 10.6% 弃用）→ 合并 → dpo_m8b（v6 数据、参考=桥、真 4 位量化训练）：M8 = ck45（验证损失最低，奖励差 0.17），M8w = ck90（奖励差 1.97 工作点） | 桥 | **v14 判负**：M8 对 M4 3:16（p=0.004）、都不要 47%；M8w 对 M8sft 4:6；真伪 M8 0/13（3 局有夸） | M8 3/19；M8w 4/10 |
| M8n | 同 M8，偏好数据去掉 v1 时代 183 对（剩 174 对）；Codex 建议的消融臂 | 桥 | 09-22 训完，存档选中第 9 步（≈桥）；M8 已判负，不出卷 | — |
| M5 / M5b | M4 桥 + dpo_v4 / v5 | 裸底座 | **废止** | 3:10；九成垃圾 |
| M6 / M6b | pt8 链 + dpo_v4 / v1 | 裸底座 | 判负（对 M4）；v11 里 M6b 赢 M7F 11:2 | 8:9 平 / 8:13；反杀 1/32 |
| 偏好数据 | dpo_v1 对照用；v4/v5 冻结；v6 待建 | | | |

---

## 五、已关闭与暂缓的路线

**已关闭**：形式服从专线 · 字数控制 · 词表裁剪 · 作者条件化 · 机械质量门 · AI 评审文学 · 四选一标注 · 小语料加大 LoRA rank · M5 线 · pt8 代加量。

**暂缓（条件触发）**：14B 全量微调——两次败诉判在 bf16 数值配置（79.56% 参数未变）；修正版 M7F 权重真正动了、同数据 dev 3.913 对 LoRA 3.856，但 **v11 主人票 0:10 判负**（09-21）：问题不是全参学得少，是学出来的东西他不要（量词泛滥）。除非 v12（同数据 M7 对 M7F）给出相反信号，不再投入。

---

## 六、裁决摘要（近 14 天，倒序；全文在 [docs/卷宗/裁决日志_全文.md](docs/卷宗/裁决日志_全文.md)）

| 日期 | 裁决 | 出处 |
|---|---|---|
| 09-23 00:45 | **v15 交付**：108 题（hh 24 / ha 24 / aa 60），三臂同底 M4sft / M4 / M4R，头条同卷对 M4。盲读否决 18：桥 12、M4 5、M4R 1——题献癖被两种偏好训练压掉。M4R 面板 214 字/循环 1.0%/比喻 0.29；偏好训练曲线在 M4 底座上一路下降（pt9 底座 45 步见底）。实验 2 第一段训练中 | [v15-人工校验记录](docs/卷宗/v15-人工校验记录.md) |
| 09-22 19:40 | **主人批准 M4R / 底座重训 / M4 尺子局三件事。** 追问「真的不是训法的问题」→ 核查：三代中文诗几乎相同（4528/4501/4588 篇），扩的是散文与译诗；训法同时改了三处（按验证损失挑存档、偏好训练修「对」后不再训动、桥数据）；同一座桥换最后一步即 M4→M5。撤回「嫌疑在语料扩大」；实验 2 改为「M4 配方不改，只加散文」，实验 1 加「桥本身」臂，都不按验证损失挑存档 | 裁决日志 09-22 19:40 |
| 09-22 19:00 | **v14 解盲：重训臂判负，三味药全无效。** M4 16 : M8 3（p=0.004），都不要 47%；M8w 对桥 4:6；真伪 M8 0/13（3 局有夸）、M4 1/7。参考接对、桥重建、v6 票——全没让主人多选。pt9 上每个臂都输给 M4；偏好训练几乎无作用。**M4 同卷都不要率 5%→47–58%（权重没变）→ 都不要率只能同卷对 M4 读**，撤回「绝对刻度」说法。下一步等裁：M4R（sft_pt6 + 修正偏好训练）/ 底座重训；v15 带 M4 尺子局与标准局 | [v14-解盲报告](docs/卷宗/v14-解盲报告.md) |
| 09-22 18:40 | **v13 解盲：参考模型接对不是灵药。** M7R 对 M7 1:2、18 局 15 局都不要（83%）；对 M4 2:5；真伪 M7R 0/11。短碎治好了票没动；按写死的读法，参考模型对错在这个水平上看不出来 | [v13-解盲报告](docs/卷宗/v13-解盲报告.md) |
| 09-22 04:10 | **v12 解盲：pt9 一代整代判负。** 机机都不要 37/48 = 77%（M7Fsft 对 M7F 21/24——不做偏好训练一样烂，偏好训练降为次因）；三臂真伪 0/40，M4 仍 14/69。有喻 4:20（p=0.002，含机口径第二次）；陆忆敏 7/8。M8 的桥与 M7 同底座 pt9_s3 → v14 是底座还是桥/偏好训练的判决卷；候选下一步「M4 桥 + 修正偏好训练」等主人 | [v12-解盲报告](docs/卷宗/v12-解盲报告.md) |
| 09-22 03:30 | **v14 重训卷交付**：80 题（ha 24 + aa 56），四臂 M8 = 参考=桥的 dpo_m8b ck45 / M8w = ck90 / M8sft = 桥 sft_pt9b / M4。头条改口只报「都不要率」，反杀与胜率降附注，过关诗册停更。预登记 M8 对 M4 >55%、M8w 对 M8sft >55%。门控 M8w 有 53% 因过长落闸（选择偏差已写明）；盲读 153 首否决 10（题献 4、伪出处 3、伪日期 2、化用名句 1）。M8n 等卡未训，不补进本卷 | [v14-人工校验记录](docs/卷宗/v14-人工校验记录.md) |
| 09-22 00:10 | **Codex 独立评审到（主人点名）**，采纳：① 修掉旧配置入口 + 启动器硬闸（quantization_bit 而 method≠bnb 拒绝起跑，已实测拒绝）；② 偏好数据消融臂 M8n（去掉 v1 时代 183 对，剩 174 对）排在 M8 之后训，v14 加 20 局 M8 对 M8n；③ 按卷分组自助区间补进总报告（无装置/长句行/有你 含机口径站得住，纯人对不站）；④ 桥数据文本分层账：训练目标与语料占比一致，模型输出把对话/记事/说理/童谣模式放大 2–5 倍——病不在数据配比，在模型退回默认模板；⑤ v14 交付附完整漏斗与三种失败短摘。未采纳/待裁：分层真人基准（需先由主人评一批新真人诗）、题献对留出（下一版）、按可数模式标签建偏好对（与「不对特征做门」冲突，请主人裁）。M8b：偏好训练验证损失第 45 步最低（奖励差仅 0.09），工作点臂 M8w = 第 90 步（奖励差 2.03）；M8 第 45 步面板循环 2.5%、字数中位 262，过线 | 外部评审_Codex_20260921、标记总报告 十一 |
| 09-21 22:00 | **循环归因收官**：底座、桥数据、训练量、退火全排除（F3 = 旧底座+旧数据+量化+8 条一批训满 = 8.1%；同底同数据 bf16 一条条累积 = 0.5–3%）→ 病因是今天因缺卡引入的「量化训练 / 8 条一批」，二者分不开。**M8 改道**：桥用已验证的 sft_pt9b，其上做参考=桥、v6 数据、验证票选存档的偏好训练；方案十条放弃三条（v5 桥数据、干净开发集早停、开发集最低点取预训练存档）。出卷题重抽（第一次抽的金标 38/40 在 v4 里）。v14 等卡：四张卡全满 | 裁决日志 09-21 22:00 |
| 09-21 18:35 | **v11 解盲：修正版全参 M7F 判负**——对 M4 0 比 10（p=0.002）、对 M6b 2 比 11（p=0.022）、反杀 0/15；都不要 31/108（机机局 52%）；两条预登记全未命中。M7F 一系不进正例池；全参路线暂缓。杨炼 8/8 单卷显著待复验。30 票并入下一版偏好数据 | v11-解盲报告 |
| 09-21 16:50 | 归因中段：300 步对照全在 10–16%（欠训压倒一切，无分辨力——教训：对照的训练量要与被比对象相同）；续训到 2 轮 7.6%；两级「开发集最低点」选中的都是循环最多的阶段（底座 7.1%→训满 3.5%；桥 11.6%→7.6%）。三条全长对照起跑（旧底座 / 旧数据 / 两者），约 2 h | 裁决日志 09-21 16:50 |
| 09-21 12:10 | **主人令：分布有明显问题必须先归因修复，不得带病出卷。v14 暂停。** M8 的桥被早停规则停在第 50 步存档，面板循环 10.6%（上一代 2.0%）。已排除量化推理、生成参数、合并再量化、桥数据；训练量（50–550 步 9–18%）与退火（短程 300 步约 10%）都不是简单原因；底座 7.1% 对 3.5% 不显著。确认的设计错误：干净开发集损失只在 ±0.005 内抖，早停是噪声叫停的，且量不到桥要学的东西。续训到 2 轮与旧底座对照在跑。副产品：偏好训练验证票在第 72 步见底，可用来选存档 | 裁决日志 09-21 12:10 |
| 09-21 06:40 | **抢卡两次溢出 → 拿卡协议**：另一位用户的批量脚本钉着每张卡、空余 ≥24000 MiB 就起下一个任务；段间一分钟空档被占走、裸抢又同时起跑，新桥两次第一步溢出。改为预热占位（只在 6 号卡）：卡一空瞬间占 7 GiB 让对方自己排队，撞车则我方让，任务装好即释放并接住段间空档，占位最长 20 分钟。末段预训练已收官：开发集末 3.8685（bf16 线 3.8737），adapter 全部张量变动 | 裁决日志 09-21 06:40 |
| 09-21 晨 | **此前的「4 位量化训练」从未发生**：配置写 `bitsandbytes`、训练库只认 `bnb`，写错不报错、整模型按 bf16 上卡；47 份训练日志无一量化，显存 28–35 GB 对真量化 12.9 GB。以前各臂训练推理都是 bf16，结论不变；「nf4 训 bf16 推无害」撤回（没发生过）。M8 改对后首次真量化训练，开发集损失高 0.024（灵敏度内）；执行器内置「日志无量化行即停」 | 归因报告 0、出厂检清单 13 |
| 09-21 | **主人指令：按溃败反思彻底改进，方法与数据的重组都由总控定，重训一版，出新卷。** 据此执行 M8 方案（十处改动：参考=桥、按验证票选存档、偏好数据 v6 398 对、题献最小对、桥数据 v5 4,329 条、干净开发集、出卷题先从训练集排除、开发集按篇重算定预训练存档、模板一致、关显存预热共卡训练）；看板 0.3 的第 1/3/4/8/10 项按总控建议先执行，主人可随时推翻 | 训练计划_M8_20260921 |
| 09-16 晚 | **剂量实验坐实「拽短来自参考模型」**：参考正确、奖励差 2.0 / 4.3 / 6.3 / 7.2 四档字数中位 283 / 322 / 340 / 296（桥 192）、短于 60 字 ≤1、碎行率 ≤3%；对照臂 37 步即短 62 字。工作点奖励差 ≈2；≥4 后撞顶三成、陈述语域降 | 归因报告 5、附录 E |
| 09-16 | 归因报告复核修订：1 成立、7 削弱、0 推翻；撤回 9 句（训到背诵、别像底座、pt8 过拟合致 55%、标点差三分之一等）；M7R 几乎没训，剂量实验起跑；五条已批做法待再裁；三份 JSON 从未入库（误报更正）；STATE 重排 | 归因报告 09-16 版、复核记录 |
| 09-08 | 实验 G：复读从预训练第 1 轮起（19%）、第 3 轮 42%、SFT 与换桥不改变（09-16 改口：48 题分不出，198 首面板下新桥 2.0% 对 6.6%）；复读门槛口径待裁 | 归因报告附录 G |
| 09-08 | 新桥 sft_pt9b 出厂检：形状最好（碎行 2.0%、短诗 3/198）；关重复罚复读 46% 与旧桥同级；已合并备做参考=桥的 DPO | 归因报告 4.3 |
| 09-08 | v13 出卷 57 题：M7R 对 M7 18、对 M4 15、真伪 24；预登记 ≥55% / ≥50%（09-16：检验力不足，读法已写死） | 卷宗 v13 记录 |
| 09-08 | 主人三裁：同意归因报告第五节做法；M7R 进卷；v11/v12 结果码晚些给 | — |
| 09-05 | 训练溃败归因：DPO 参考=裸底座、dpo_v1 构成、训练强度、7 轮、复读、模板六条（09-16 复核修订见上） | 归因报告 |
| 09-04 | v10b 解盲：M6b 对 M4 8:13、都不要 55%；pt8 代路线止；两份总报告全量重编（1,312 对次、170 条批注） | 卷宗 v10b、总答卷 0904 |
| 09-02 | 主人裁决 M5 线废止；M7F/M7 全链收官；v10 拆 v10b、v11/v12 出卷；dpo_v1 拽短在两底座复验（09-05 改判为参考模型接错） | 卷宗 v10–v12 记录 |
