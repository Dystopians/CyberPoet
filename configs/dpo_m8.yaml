### M8 的偏好训练（09-21）：底座 = 已合并新桥的完整权重 merged_sft_m8（nf4 加载）+ 新建 LoRA ⇒ 参考模型 = 桥本身；
### 数据 = 偏好数据 v6（训练 357 对 / 验证 41 对，按题面不跨集）；lr 2e-5（09-16 剂量实验的档位）、β 0.1、3 轮，每 9 步（0.2 轮）评一次并存档；
### 选存档规则（训前写死）：验证损失最低者；并列（差 <0.01）取更早的；面板字数中位须在 120–300 之间，否则向更早一档退。
model_name_or_path: /data/peilincai/CyberPoetTraining/claude_night_20260827/outputs/merged_sft_m8
trust_remote_code: true
quantization_bit: 4
quantization_method: bnb   # 09-21：库里的枚举值是 bnb；写 bitsandbytes 不报错、直接不量化（此前 93 份配置全中招）
quantization_type: nf4
double_quantization: true
flash_attn: auto

stage: dpo
pref_beta: 0.1
pref_loss: sigmoid
do_train: true
finetuning_type: lora
lora_target: all
lora_rank: 32
lora_alpha: 64
lora_dropout: 0.05

dataset_dir: /data/peilincai/CyberPoetTraining/claude_night_20260827/data
dataset: cyberpoet_dpo_v6
eval_dataset: cyberpoet_dpo_v6_val
template: qwen3
enable_thinking: false
cutoff_len: 2048
preprocessing_num_workers: 24
overwrite_cache: true

output_dir: /data/peilincai/CyberPoetTraining/claude_night_20260827/outputs/dpo_m8
logging_steps: 3
save_strategy: steps
save_steps: 9
eval_strategy: steps
eval_steps: 9
per_device_eval_batch_size: 1
save_total_limit: 20
save_only_model: true
plot_loss: true
report_to: none
run_name: cyberpoet-dpo_m8

per_device_train_batch_size: 1
gradient_accumulation_steps: 8
learning_rate: 2.0e-5
num_train_epochs: 3.0
lr_scheduler_type: cosine
warmup_ratio: 0.1
bf16: true
tf32: true
gradient_checkpointing: true
gradient_checkpointing_kwargs:
  use_reentrant: false
optim: paged_adamw_8bit
max_grad_norm: 1.0
seed: 20260825
