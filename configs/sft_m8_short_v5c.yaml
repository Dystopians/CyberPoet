### 归因对照 X4：与 X3（sft_m8_short_bs1：新底座、4 位量化、300 步退火、1×8）完全相同，只把数据换成 v5c = v5 + 600 条续写行（题面含「不重复前文」）→ 隔离「删掉续写行」。
model_name_or_path: models/Qwen3-14B
adapter_name_or_path: /data/peilincai/CyberPoetTraining/claude_night_20260827/outputs/pt10_s3
create_new_adapter: false
trust_remote_code: true
quantization_bit: 4
quantization_method: bnb   # 09-21：库里的枚举值是 bnb；写 bitsandbytes 不报错、直接不量化（此前 93 份配置全中招）
quantization_type: nf4
double_quantization: true
flash_attn: auto
enable_liger_kernel: true

stage: sft
do_train: true
finetuning_type: lora
lora_target: all
lora_rank: 32
lora_alpha: 64
lora_dropout: 0.05

dataset_dir: /data/peilincai/CyberPoetTraining/claude_night_20260827/data
dataset: cyberpoet_sft_v5c_train
eval_dataset: cyberpoet_sft_v5_dev
template: qwen3
enable_thinking: false
cutoff_len: 2048
packing: false
train_on_prompt: false
preprocessing_num_workers: 24
overwrite_cache: true

output_dir: /data/peilincai/CyberPoetTraining/claude_night_20260827/outputs/sft_m8_short_v5c
logging_steps: 10
save_strategy: steps
save_steps: 100
eval_steps: 50
save_total_limit: 10
save_only_model: false
metric_for_best_model: eval_loss
greater_is_better: false
plot_loss: true
report_to: none
run_name: cyberpoet-sft_m8_short_v5c

per_device_train_batch_size: 1
gradient_accumulation_steps: 8
learning_rate: 2.0e-5
max_steps: 300
lr_scheduler_type: cosine
warmup_ratio: 0.05
bf16: true
tf32: true
gradient_checkpointing: true
gradient_checkpointing_kwargs:
  use_reentrant: false
optim: paged_adamw_8bit
weight_decay: 0.01
max_grad_norm: 1.0
seed: 20260825

per_device_eval_batch_size: 1
eval_strategy: steps
load_best_model_at_end: false
